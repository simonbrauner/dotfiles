#+title: X.org
#+property: header-args :mkdirp yes

Not wayland.

* X11

X Window System implemetation [[https://wiki.archlinux.org/title/xorg][Xorg]].

** Display server

[[https://wiki.archlinux.org/title/Xinit#Autostart_X_at_login][Starting X]] after logging in to bash.

#+begin_src shell :tangle ~/.bash_profile
start_window_manager() {
    rm ~/.xinitrc
    ln -s $1 ~/.xinitrc
    exec startx
}

if [ -z "${DISPLAY}" ]; then
    case "${XDG_VTNR}" in
        1)
            start_window_manager ~/dotfiles/X/.exwm_xinitrc
            ;;
        3)
            start_window_manager ~/dotfiles/X/.xmonad_xinitrc
            ;;
    esac
fi
#+end_src

** Executing window manager

Running xmonad...

#+begin_src shell :tangle .xmonad_xinitrc
exec xmonad
#+end_src

or [[https://github.com/ch11ng/exwm/blob/master/xinitrc][EXWM]]...

#+begin_src shell :tangle .exwm_xinitrc
xhost +SI:localuser:$USER

export _JAVA_AWT_WM_NONREPARENTING=1

xsetroot -cursor_name left_ptr

xset r rate 200 50

exec emacs
#+end_src

...with [[https://wiki.archlinux.org/title/Xinit][xinit]].

** Keyboard layout

US [[https://wiki.archlinux.org/title/Xorg/Keyboard_configuration#Using_X_configuration_files][keyboard layout]], czech characters with =AltGr=.

#+begin_src conf :tangle /sudo::/etc/X11/xorg.conf.d/00-keyboard.conf
Section "InputClass"
    Identifier "system-keyboard"
    MatchIsKeyboard "on"
        Option "XkbLayout" "cz"
        Option "XkbVariant" "coder"
EndSection
#+end_src

** Touchpad

Tapping the [[https://wiki.archlinux.org/title/Touchpad_Synaptics#Configuration][touchpad]] works as a mouse click, right button in the right top corner.

#+begin_src conf :tangle /sudo::/etc/X11/xorg.conf.d/70-synaptics.conf
Section "InputClass"
    Identifier "touchpad"
    Driver "synaptics"
    MatchIsTouchpad "on"
        Option "TapButton1" "1"
        Option "RTCornerButton" "3"
EndSection
#+end_src

** Package

#+begin_src text
xf86-input-synaptics
#+end_src
