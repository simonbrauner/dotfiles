#+title: User.org
#+property: header-args :mkdirp yes
#+property: header-args:text :tangle ./packages/user_pkglist.txt

User applications and their configuration.

* Git

Version control system [[https://wiki.archlinux.org/title/git][Git]].

*** Config file

#+begin_src conf :tangle ~/.gitconfig
[user]
name = Šimon Brauner
email = simonbrauner@seznam.cz

[core]
editor = emacs

[alias]
graph = log --all --oneline --graph
#+end_src

* SSH

Connecting to other devices with [[https://wiki.archlinux.org/title/OpenSSH][OpenSSH]].

*** Keys

[[https://docs.github.com/en/github/authenticating-to-github/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent][New key]].

#+begin_src shell
ssh-keygen -t ed25519 -C "simonbrauner@seznam.cz"
#+end_src

*** Package

#+begin_src text
openssh
#+end_src

* Chromium

A web browser.

*** Package

#+begin_src text
chromium
#+end_src

* Feh

A light-weight, configurable and versatile image viewer - [[https://wiki.archlinux.org/title/feh][Feh]].

*** Package

#+begin_src text
feh
#+end_src

* VLC

Video player [[https://wiki.archlinux.org/title/VLC_media_player][VLC]].

*** Package

#+begin_src text
vlc
#+end_src
