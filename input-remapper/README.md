# Input Remapper

## Broken keys

`~/.config/input-remapper-2/presets/AT\ Translated\ Set\ 2\ keyboard/broken\ keys.json`

Simulates key presses of `G`, `H`, `'`, `up arrow` and `esc` after pressing `🪟+F`, `🪟+J`, `🪟+;`, `right shift` and `caps lock` respectively.
