# Input Remapper

## Broken keys

`~/.config/input-remapper-2/presets/AT\ Translated\ Set\ 2\ keyboard/broken\ keys.json`

Simulates key presses of `G`, `H`, `'` and `⬆️` after pressing `🪟+F`, `🪟+J`, `🪟+;` and `right shift` respectively.
